[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "test-protos"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = ["protobuf>=4.21.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/test_api", "src/test_internal"]
